<nav class="z-5 bg-gray-700">
  <ul class="flex flex-col sm:flex-row text-white justify-center">
    <li>
      <a class="hover:bg-gray-900 transition-all py-4 px-5 block uppercase" href="{{ path('app_home') }}">Accueil</a>
    </li>

    {% if app.user %}
      <li>
        <a class="hover:bg-gray-900 transition-all py-4 px-5 block uppercase" href="{{ path('app_login') }}">Logout</a>
      </li>
    {% else %}
      <li>
        <a class="hover:bg-gray-900 transition-all py-4 px-5 block uppercase" href="{{ path('app_login') }}">Login</a>
      </li>
    {% endif %}
  </ul>
</nav>
