{% extends 'base.html.twig' %}

{% block title %}
  Proposer une nouvelle langue
{% endblock %}

{% block body %}
  <h1>Proposer une nouvelle langue</h1>

  {{ form_start(form) }}
  {{ form_row(form.name) }}
  {{ form_row(form.description) }}
  {# {{ form_row(form.submittedAt) }} #}
  {# <button class="btn btn-primary">Envoyer</button> #}
  {{ form_end(form) }}
{% endblock %}
