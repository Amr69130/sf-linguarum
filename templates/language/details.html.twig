{% extends 'base.html.twig' %}

{% block title %}
  {{ language.name }}
{% endblock %}

{% block body %}
  <div class="min-h-screen bg-gray-100 flex flex-col justify-center items-center p-8">
    <div class="bg-white shadow-xl rounded-2xl p-10 max-w-xl w-full text-center">
      <h1 class="text-4xl font-bold text-indigo-600 mb-4">{{ language.name }}</h1>
      <p class="text-gray-700 text-lg mb-6">{{ language.description }}</p>

      {% if language.parent %}
        <p class="text-sm text-gray-500 mb-4">
          🌱 Langue mère : <strong>{{ language.parent.name }}</strong>
        </p>
      {% endif %}

      <a href="{{ path('app_home') }}" class="inline-block mt-6 text-indigo-500 hover:underline">← Retour à l'accueil</a>
    </div>
  </div>
{% endblock %}
